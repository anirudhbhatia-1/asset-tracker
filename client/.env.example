# API Base URL (uses Vite proxy in dev)
VITE_API_BASE_URL=/api

# Google Identity Services client ID. Required to show Google login in production
# and must exactly match the server GOOGLE_CLIENT_ID value.
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
