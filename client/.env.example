# API Base URL (uses Vite proxy in dev)
VITE_API_BASE_URL=/api

# Google OAuth Client ID (Testing only — same value as server GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
